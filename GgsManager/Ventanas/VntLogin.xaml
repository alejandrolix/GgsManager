<Window x:Class="VntLogin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GgsManager"
        mc:Ignorable="d"
        Title="Inicio de Sesión" Height="350.427" Width="555.102"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="Etiquetas">
            <Setter Property="FontSize" Value="17" />
        </Style>

        <Style TargetType="TextBox" x:Key="TextBox">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Width" Value="144" />
        </Style>
    </Window.Resources>

    <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid Height="90">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="150" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="43" />
                <RowDefinition Height="60" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Usuario" Style="{StaticResource Etiquetas}" VerticalAlignment="Center" />
            <TextBox Grid.Row="0" Grid.Column="1" Name="UsuarioTxt" Style="{StaticResource TextBox}" VerticalAlignment="Center" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Contraseña" Style="{StaticResource Etiquetas}" VerticalAlignment="Center" />
            <TextBox Grid.Row="1" Grid.Column="1" Name="PasswordTxt" Style="{StaticResource TextBox}" VerticalAlignment="Center" />
        </Grid>

        <Button Name="IniciarSesion" Content="Iniciar Sesión" Width="110" Height="28" Margin="0,46,0,0" FontSize="15" />
    </StackPanel>
</Window>
